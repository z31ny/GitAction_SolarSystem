apiVersion: v1
kind: Service
metadata:
  name: {{ include "solar-system.fullname" . }}-svc
spec:
  selector:
    app: {{ include "solar-system.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
