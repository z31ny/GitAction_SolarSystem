{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "solar-system.fullname" .) }}
  labels:
    {{- include "solar-system.labels" . | nindent 4 }}
{{- end }}
